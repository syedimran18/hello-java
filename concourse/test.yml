---
platform: linux

container_limits: {}

image_resource:
  type: registry-image
  source:
    repository: maven
    tag: 3.6.3

caches:
  - path: src/.m2

inputs:
  - name: src

outputs:
  - name: build

params:
  app_name:

run:
  path: bash
  args:
  - -c mvn clean test
